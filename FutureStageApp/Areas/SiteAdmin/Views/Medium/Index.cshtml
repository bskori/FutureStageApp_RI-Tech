@model IEnumerable<Medium>

    <div class="row">
        <div class="col-md-12">
            <br />
            <br />
            <h3 class="text-center alert-primary">Medium List</h3>
            <br />
            <p>
                <a asp-action="Create" class="btn btn-primary">Create New</a>
            </p>
            <table class="table table-striped table-hover">
                <tr class="thead-dark">
                    <th>Medium ID</th>
                    <th>Medium Name</th>
                    <th></th>
                </tr>
                @foreach (var temp in Model)
                {
                    <tr>
                        <td>@temp.MediumID</td>
                        <td>@temp.MediumTitle</td>

                        <td>
                            <a class="btn btn-success" asp-action="Edit" asp-route-id="@temp.MediumID">Edit</a>

                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@temp.MediumID">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>




