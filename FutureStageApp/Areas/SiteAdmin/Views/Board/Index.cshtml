@model IEnumerable<EducationBoard>

    <div class="row">
        <div class="col-md-12">
            <br />
            <br />
            <h3 class="text-center alert-primary">Board List</h3>
            <br />
            <p>
                <a asp-action="Create" class="btn btn-primary">Create New</a>
            </p>
            <table class="table table-striped table-hover">
                <tr class="thead-dark">
                    <th>Board ID</th>
                    <th>Board Title</th>
                    <th>Board Description</th>
                    <th></th>
                </tr>
                @foreach (var temp in Model)
                {
                    <tr>
                        <td>@temp.EducationBoardID</td>
                        <td>@temp.EducationBoardTitle</td>
                        <td>@temp.EducationBoardDesc</td>

                        <td>
                            <a class="btn btn-success" asp-action="Edit" asp-route-id="@temp.EducationBoardID">Edit</a>

                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@temp.EducationBoardID">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>




